import express from 'express';
import http from 'http';
import crypto from 'crypto';
import fs from 'fs';
import path from 'path';
import { WebSocketServer,WebSocket } from 'ws';
const app=express(),server=http.createServer(app),wss=new WebSocketServer({server,path:'/events'});
const PORT=Number(process.env.PORT||3000),PASS=String(process.env.ADMIN_PASSWORD||''),PROD=process.env.NODE_ENV==='production';
const DATA_DIR=process.env.DATA_DIR||path.join(process.cwd(),'data'),STATE_FILE=path.join(DATA_DIR,'state.json');
app.use(express.json({limit:'160mb'}));app.use(express.static('.',{index:'index.html'}));
const FONT_IDS=['Bangers','Luckiest Guy','Bowlby One SC','Black Ops One','Russo One','Titan One','Anton','Lilita One'];
function defaults(){return{battle:{title:'BATALHA DAS OPÇÕES',subtitle:'Envie presentes para a sua opção favorita!',backgroundImage:'',titleFont:'Bangers',titleSize:40,championLabel:'★ VENCEDOR DA BATALHA ★',showTimer:false,commentVoting:false},timer:{elapsedMs:0,running:false,startedAt:null},options:[{id:crypto.randomUUID(),name:'Opção A',image:'',giftIcon:'',color:'#ffcc00',gifts:'Rosa, Coração',giftIds:'',count:0},{id:crypto.randomUUID(),name:'Opção B',image:'',giftIcon:'',color:'#9aa7b3',gifts:'Café, Perfume',giftIds:'',count:0}],lastGift:null,giftHistory:[],giftCatalog:[],savedPresets:[]}}
function load(){try{const saved=JSON.parse(fs.readFileSync(STATE_FILE,'utf8')),d=defaults();return{...d,...saved,battle:{...d.battle,...(saved.battle||{}),showTimer:false},timer:{elapsedMs:Math.max(0,Number(saved.timer?.elapsedMs)||0),running:false,startedAt:null},giftHistory:Array.isArray(saved.giftHistory)?saved.giftHistory:[],giftCatalog:Array.isArray(saved.giftCatalog)?saved.giftCatalog:[],savedPresets:Array.isArray(saved.savedPresets)?saved.savedPresets:[]}}catch{return defaults()}}let state=load();
function persist(){try{fs.mkdirSync(DATA_DIR,{recursive:true});fs.writeFileSync(STATE_FILE,JSON.stringify(state),'utf8')}catch(e){console.error('Não foi possível persistir estado:',e.message)}}
function auth(req,res,next){if(!PASS&&!PROD)return next();if(!PASS)return res.status(503).send('ADMIN_PASSWORD não configurada');const h=req.headers.authorization||'';if(!h.startsWith('Basic ')){res.set('WWW-Authenticate','Basic realm="Batalha Admin"');return res.status(401).end()}try{const d=Buffer.from(h.slice(6),'base64').toString(),i=d.indexOf(':'),u=d.slice(0,i),p=d.slice(i+1),a=Buffer.from(p),b=Buffer.from(PASS);if(u==='admin'&&a.length===b.length&&crypto.timingSafeEqual(a,b))return next()}catch{}res.set('WWW-Authenticate','Basic realm="Batalha Admin"');res.status(401).end()}
function send(type,payload={}){const msg=JSON.stringify({type,...payload,at:Date.now()});for(const ws of wss.clients)if(ws.readyState===WebSocket.OPEN)ws.send(msg)}function norm(s=''){return String(s).normalize('NFD').replace(/[\u0300-\u036f]/g,'').trim().toLowerCase()}function giftNames(s=''){return String(s).split(',').map(norm).filter(Boolean)}function giftIds(s=''){return String(s).split(',').map(x=>String(x).trim()).filter(Boolean)}
function cleanOption(o={}){return{id:String(o.id||crypto.randomUUID()),name:String(o.name||'Opção').trim().slice(0,80),image:String(o.image||'').slice(0,12000000),giftIcon:String(o.giftIcon||'').slice(0,5000000),color:/^#[0-9a-f]{6}$/i.test(String(o.color||''))?String(o.color):'#2f7cff',gifts:String(o.gifts||'').trim().slice(0,1500),giftIds:String(o.giftIds||'').replace(/[^0-9, ]/g,'').slice(0,1500),count:Math.max(0,Number(o.count)||0)}}
function cleanBattle(b={}){const f=FONT_IDS.includes(String(b.titleFont))?String(b.titleFont):'Bangers',sz=Math.max(24,Math.min(48,Number(b.titleSize)||40));return{title:String(b.title||'BATALHA DAS OPÇÕES').trim().slice(0,100),subtitle:String(b.subtitle||'Envie presentes para a sua opção favorita!').trim().slice(0,180),backgroundImage:String(b.backgroundImage||'').slice(0,12000000),titleFont:f,titleSize:sz,championLabel:String(b.championLabel||'★ VENCEDOR DA BATALHA ★').trim().slice(0,80),showTimer:b.showTimer===true,commentVoting:b.commentVoting===true}}
function validateGiftMap(options){const usedNames=new Map(),usedIds=new Map();for(const o of options){for(const g of giftNames(o.gifts)){if(usedNames.has(g)&&usedNames.get(g)!==o.name)return `O presente "${g}" está configurado em mais de uma opção.`;usedNames.set(g,o.name)}for(const id of giftIds(o.giftIds)){if(usedIds.has(id)&&usedIds.get(id)!==o.name)return `O Gift ID "${id}" está configurado em mais de uma opção.`;usedIds.set(id,o.name)}}return null}function validateOptionNames(options){const used=new Map();for(const o of options){const n=norm(o.name);if(!n)return'Todas as opções precisam ter nome.';if(used.has(n))return`Existem duas opções com o mesmo nome: "${o.name}".`;used.set(n,true)}return null}
function timerSnapshot(){return{elapsedMs:state.timer.elapsedMs+(state.timer.running&&state.timer.startedAt?Date.now()-state.timer.startedAt:0),running:state.timer.running,startedAt:state.timer.startedAt}}
function countSnapshot(){return state.options.map(o=>({id:o.id,count:Math.max(0,Number(o.count)||0)}))}
function registerGift(data={}){const giftName=String(data.giftName||'').trim(),giftId=String(data.giftId||'').trim();if(!giftName&&!giftId)return{matched:false};let opt=giftId?state.options.find(o=>giftIds(o.giftIds).includes(giftId)):null;if(!opt&&giftName)opt=state.options.find(o=>giftNames(o.gifts).includes(norm(giftName)));if(!opt)return{matched:false,giftName,giftId};const repeat=Math.max(1,Number(data.repeatCount||1)||1),u=data.user||{},entry={id:crypto.randomUUID(),source:'gift',optionId:opt.id,optionName:opt.name,giftId,giftName:giftName||`Gift ${giftId}`,giftImage:String(data.giftImage||''),repeatCount:repeat,user:{uniqueId:String(u.uniqueId||''),userId:String(u.userId||''),nickname:String(u.nickname||u.uniqueId||'TikTok'),avatar:String(u.avatar||'')},at:Date.now()};opt.count+=repeat;state.lastGift=entry;state.giftHistory.unshift(entry);if(state.giftHistory.length>500)state.giftHistory.length=500;persist();send('gift',{counts:countSnapshot(),timer:timerSnapshot(),lastGift:entry});return{matched:true,option:opt,lastGift:entry}}
function registerCommentVote(data={}){if(state.battle.commentVoting!==true)return{matched:false,disabled:true};const comment=String(data.comment||'').trim();if(!comment)return{matched:false};const opt=state.options.find(o=>norm(o.name)===norm(comment));if(!opt)return{matched:false,comment};const u=data.user||{},entry={id:crypto.randomUUID(),source:'comment',optionId:opt.id,optionName:opt.name,giftId:'',giftName:'Comentário',giftImage:'',repeatCount:1,comment,user:{uniqueId:String(u.uniqueId||''),userId:String(u.userId||''),nickname:String(u.nickname||u.uniqueId||'TikTok'),avatar:String(u.avatar||'')},at:Date.now()};opt.count++;state.giftHistory.unshift(entry);if(state.giftHistory.length>500)state.giftHistory.length=500;persist();send('commentVote',{counts:countSnapshot(),timer:timerSnapshot(),vote:entry});return{matched:true,option:opt,vote:entry}}
function saveCatalog(data={}){const incoming=Array.isArray(data.gifts)?data.gifts:[],byId=new Map((state.giftCatalog||[]).map(g=>[String(g.id),g]));let added=0,updated=0;for(const raw of incoming){const g={id:String(raw.id||raw.giftId||'').trim(),name:String(raw.name||raw.giftName||'').trim(),image:String(raw.image||'')};if(!g.id||!g.name)continue;const old=byId.get(g.id);if(!old){byId.set(g.id,g);added++}else{byId.set(g.id,{...old,...g,image:g.image||old.image||''});updated++}}state.giftCatalog=[...byId.values()].sort((a,b)=>a.name.localeCompare(b.name,'pt-BR')).slice(0,3000);persist();send('giftCatalog',{giftCatalog:state.giftCatalog});return{ok:true,count:state.giftCatalog.length,added,updated}}
function presetSummary(){return(state.savedPresets||[]).map(p=>({name:p.name,updatedAt:p.updatedAt})).sort((a,b)=>b.updatedAt-a.updatedAt)}
app.get('/api/health',(req,res)=>res.json({ok:true,version:19}));app.get('/api/state',(req,res)=>res.json({...state,timer:timerSnapshot()}));app.get('/api/gifts',auth,(req,res)=>res.json({ok:true,gifts:state.giftCatalog||[]}));app.get('/api/gift-history',auth,(req,res)=>res.json({ok:true,history:state.giftHistory||[]}));app.get('/api/presets',auth,(req,res)=>res.json({ok:true,presets:presetSummary()}));
app.post('/api/relay',auth,(req,res)=>{const event=String(req.body?.event||'');if(event==='gift')return res.json({ok:true,...registerGift(req.body?.data||{})});if(event==='comment')return res.json({ok:true,...registerCommentVote(req.body?.data||{})});if(event==='giftCatalog')return res.json(saveCatalog(req.body?.data||{}));return res.status(400).json({ok:false,error:'Evento não suportado'})});
app.post('/api/config',auth,(req,res)=>{const raw=Array.isArray(req.body?.options)?req.body.options:[];if(!raw.length)return res.status(400).json({ok:false,error:'Crie pelo menos uma opção'});if(raw.length>6)return res.status(400).json({ok:false,error:'O máximo é 6 opções'});const options=raw.map(cleanOption),battle=cleanBattle(req.body?.battle||{}),mapError=validateGiftMap(options);if(mapError)return res.status(400).json({ok:false,error:mapError});if(battle.commentVoting===true){const nameError=validateOptionNames(options);if(nameError)return res.status(400).json({ok:false,error:nameError})}else for(const o of options)if(!norm(o.name))return res.status(400).json({ok:false,error:'Todas as opções precisam ter nome.'});state.battle=battle;state.options=options;persist();send('state',{...state,timer:timerSnapshot()});res.json({ok:true,savedOptions:state.options.length,...state,timer:timerSnapshot()})});
app.post('/api/presets/save',auth,(req,res)=>{const battle=cleanBattle(req.body?.battle||state.battle),raw=Array.isArray(req.body?.options)?req.body.options:state.options,name=String(battle.title||'').trim();if(!name)return res.status(400).json({ok:false,error:'Informe o Nome da batalha antes de salvar.'});if(!raw.length)return res.status(400).json({ok:false,error:'Crie pelo menos uma opção.'});const options=raw.slice(0,6).map(o=>({...cleanOption(o),count:0})),preset={name,battle,options,updatedAt:Date.now()};state.savedPresets=Array.isArray(state.savedPresets)?state.savedPresets:[];const i=state.savedPresets.findIndex(p=>norm(p.name)===norm(name));if(i>=0)state.savedPresets[i]=preset;else state.savedPresets.push(preset);if(state.savedPresets.length>100)state.savedPresets=state.savedPresets.sort((a,b)=>b.updatedAt-a.updatedAt).slice(0,100);persist();res.json({ok:true,replaced:i>=0,name,presets:presetSummary()})});
app.post('/api/presets/restore',auth,(req,res)=>{const name=String(req.body?.name||'').trim(),preset=(state.savedPresets||[]).find(p=>norm(p.name)===norm(name));if(!preset)return res.status(404).json({ok:false,error:'Configuração salva não encontrada.'});state.battle=cleanBattle(preset.battle);state.options=(preset.options||[]).map(o=>({...cleanOption(o),count:0}));state.lastGift=null;state.giftHistory=[];state.timer={elapsedMs:0,running:false,startedAt:null};persist();send('state',{...state,timer:timerSnapshot()});res.json({ok:true,name:preset.name,...state,timer:timerSnapshot()})});
app.post('/api/reset',auth,(req,res)=>{for(const o of state.options)o.count=0;state.lastGift=null;state.giftHistory=[];persist();send('state',{...state,timer:timerSnapshot()});res.json({ok:true})});app.post('/api/reset-all',auth,(req,res)=>{const catalog=state.giftCatalog||[],savedPresets=state.savedPresets||[];state=defaults();state.giftCatalog=catalog;state.savedPresets=savedPresets;persist();send('state',{...state,timer:timerSnapshot()});res.json({ok:true,...state,timer:timerSnapshot()})});
app.post('/api/manual',auth,(req,res)=>{const id=String(req.body?.id||''),amount=Math.max(1,Math.min(999,Number(req.body?.amount||1))),opt=state.options.find(o=>o.id===id);if(!opt)return res.status(404).json({ok:false,error:'Opção não encontrada'});opt.count+=amount;const giftName=String(opt.gifts||'').split(',')[0]?.trim()||'Presente de teste',giftId=giftIds(opt.giftIds)[0]||'admin-test',entry={id:crypto.randomUUID(),source:'gift',optionId:opt.id,optionName:opt.name,giftId,giftName,giftImage:opt.giftIcon||'',repeatCount:amount,user:{uniqueId:'admin-teste',userId:'admin-teste',nickname:'Teste Admin',avatar:''},at:Date.now()};state.lastGift=entry;persist();send('gift',{counts:countSnapshot(),timer:timerSnapshot(),lastGift:entry});res.json({ok:true,option:opt,lastGift:entry})});
app.post('/api/timer',auth,(req,res)=>{const action=String(req.body?.action||'');if(action==='play'){if(!state.timer.running){state.timer.running=true;state.timer.startedAt=Date.now()}}else if(action==='pause'){if(state.timer.running&&state.timer.startedAt)state.timer.elapsedMs+=Date.now()-state.timer.startedAt;state.timer.running=false;state.timer.startedAt=null}else if(action==='reset'){state.timer.elapsedMs=0;state.timer.running=false;state.timer.startedAt=null}else return res.status(400).json({ok:false,error:'Ação inválida'});persist();const timer=timerSnapshot();send('timer',{timer});res.json({ok:true,timer})});app.post('/api/champion',auth,(req,res)=>{if(!state.options.length)return res.status(400).json({ok:false,error:'Sem opções'});const max=Math.max(...state.options.map(o=>Number(o.count)||0)),leaders=state.options.filter(o=>(Number(o.count)||0)===max);if(max<=0)return res.status(400).json({ok:false,error:'Ainda não há pontos'});if(leaders.length!==1)return res.status(400).json({ok:false,error:'Há empate no primeiro lugar'});const champion={...leaders[0],championLabel:state.battle.championLabel};send('champion',{champion});res.json({ok:true,champion})});
const PRESET_UI=`<style>#presetBox{max-width:1280px;margin:0 auto 24px;padding:0 16px}#presetBox .preset-card{background:#102433;border:1px solid #ffffff10;border-radius:18px;padding:16px;color:#fff;font-family:Arial,sans-serif}#presetBox h2{margin:0 0 4px;font-size:17px}#presetBox p{margin:0 0 12px;color:#8ea6b7;font-size:11px}#presetBox .preset-grid{display:grid;grid-template-columns:1fr auto auto;gap:8px}#presetBox select,#presetBox button{border:1px solid #ffffff12;border-radius:10px;padding:10px;background:#07141d;color:#fff;font-weight:900}#presetBox button{cursor:pointer}#presetSave{background:#176b3b!important}#presetRestore{background:#155f87!important}@media(max-width:650px){#presetBox .preset-grid{grid-template-columns:1fr}}</style><div id="presetBox"><div class="preset-card"><h2>💾 Configurações salvas</h2><p>O Nome da batalha é usado como nome do salvamento. Se já existir, será substituído.</p><div class="preset-grid"><select id="presetSelect"><option value="">Selecione uma configuração para restaurar</option></select><button id="presetSave">SALVAR CONFIGURAÇÃO ATUAL</button><button id="presetRestore">RESTAURAR SELECIONADA</button></div></div></div><script>(function(){const sel=document.getElementById('presetSelect'),btnS=document.getElementById('presetSave'),btnR=document.getElementById('presetRestore');async function refresh(){try{const r=await fetch('/api/presets'),d=await r.json(),cur=sel.value;sel.innerHTML='<option value="">Selecione uma configuração para restaurar</option>'+((d.presets||[]).map(p=>'<option value="'+String(p.name).replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;')+'">'+String(p.name).replace(/&/g,'&amp;').replace(/</g,'&lt;')+'</option>').join(''));if([...sel.options].some(o=>o.value===cur))sel.value=cur}catch{}}btnS.onclick=async()=>{try{const data=typeof payload==='function'?payload():{battle:{title:document.getElementById('title')?.value||''},options:typeof options!=='undefined'?options:[]};const r=await fetch('/api/presets/save',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)}),d=await r.json();if(!r.ok)throw Error(d.error||'Falha ao salvar');if(document.getElementById('msg'))document.getElementById('msg').textContent=(d.replaced?'♻️ Substituída: ':'✅ Salva: ')+d.name;await refresh();sel.value=d.name}catch(e){if(document.getElementById('msg'))document.getElementById('msg').textContent='⚠️ '+e.message}};btnR.onclick=async()=>{try{if(!sel.value)throw Error('Selecione uma configuração.');const r=await fetch('/api/presets/restore',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:sel.value})}),d=await r.json();if(!r.ok)throw Error(d.error||'Falha ao restaurar');if(typeof applyState==='function')applyState(d);if(document.getElementById('msg'))document.getElementById('msg').textContent='✅ Restaurada: '+d.name}catch(e){if(document.getElementById('msg'))document.getElementById('msg').textContent='⚠️ '+e.message}};refresh()})();</script>`;
app.get(['/admin','/admin.html'],auth,(req,res)=>{try{const html=fs.readFileSync(new URL('./admin.html',import.meta.url),'utf8');res.type('html').send(html.replace('</body>',PRESET_UI+'</body>'))}catch{res.status(500).send('Erro ao carregar Admin')}});wss.on('connection',ws=>ws.send(JSON.stringify({type:'state',...state,timer:timerSnapshot()})));server.listen(PORT,'0.0.0.0',()=>{persist();console.log(`Batalha de Opções rodando na porta ${PORT}`)});